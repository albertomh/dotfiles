#!/usr/bin/env bash
# Re-runs whenever the Brewfile changes, because its hash is embedded here:
# Brewfile hash: {{ include "Brewfile" | sha256sum }}
set -euo pipefail
eval "$(/opt/homebrew/bin/brew shellenv)"
brew bundle --file="{{ .chezmoi.sourceDir }}/Brewfile"
